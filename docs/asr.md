# ASR

The assistant uses `mlx-community/whisper-small-mlx` through MLX Whisper.

The main runtime is started with `./start_assistant.sh`. Press `Control+Command+Z` to start recording and press it again to stop and transcribe. macOS may require Microphone and Accessibility permission for the host application.
